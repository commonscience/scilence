{
  "name": "@scilence/primitives",
  "version": "0.1.0",
  "description": "STEAMCO design system primitives — vanilla TS/JS atoms + composite primitives, framework-agnostic, consumable from any STEAMCO surface",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "src",
    "README.md"
  ],
  "license": "MIT",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -p tsconfig.json --watch",
    "clean": "rm -rf dist",
    "test": "echo \"(scaffold) no tests yet — first primitive will add real tests\""
  },
  "publishConfig": {
    "access": "public"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
